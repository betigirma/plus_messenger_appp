import 'package:flutter/cupertino.dart';

class ChatscountersPage extends StatelessWidget {
  const ChatscountersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('chat counter'));
  }
}
