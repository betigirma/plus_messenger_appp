import 'package:flutter/cupertino.dart';

class DownloadthemesPage extends StatelessWidget {
  const DownloadthemesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('download themes'));
  }
}
