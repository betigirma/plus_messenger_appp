import 'package:flutter/material.dart';

class Plus extends StatelessWidget {
  final String title;
  final String subtitle;
  const Plus({super.key, required this.title, required this.subtitle});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      scrollDirection: Axis.vertical,
      child: Column(
        children: [
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
          const SizedBox(
            height: 5,
            width: 450,
            child: Divider(color: Colors.blueGrey),
          ),
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
          const SizedBox(
            height: 5,
            width: 450,
            child: Divider(color: Colors.blueGrey),
          ),
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
          const SizedBox(
            height: 5,
            width: 450,
            child: Divider(color: Colors.blueGrey),
          ),
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
          const SizedBox(
            height: 5,
            width: 450,
            child: Divider(color: Colors.blueGrey),
          ),
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
          const SizedBox(
            height: 5,
            width: 450,
            child: Divider(color: Colors.blueGrey),
          ),
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
          const SizedBox(
            height: 5,
            width: 450,
            child: Divider(color: Colors.blueGrey),
          ),
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
          const SizedBox(
            height: 5,
            width: 450,
            child: Divider(color: Colors.blueGrey),
          ),
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
          const SizedBox(
            height: 5,
            width: 450,
            child: Divider(color: Colors.blueGrey),
          ),
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
          const SizedBox(
            height: 5,
            width: 450,
            child: Divider(color: Colors.blueGrey),
          ),
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
          const SizedBox(
            height: 5,
            width: 450,
            child: Divider(color: Colors.blueGrey),
          ),
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
          const SizedBox(
            height: 5,
            width: 450,
            child: Divider(color: Colors.blueGrey),
          ),
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
          const SizedBox(
            height: 5,
            width: 450,
            child: Divider(color: Colors.blueGrey),
          ),
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
          const SizedBox(
            height: 5,
            width: 450,
            child: Divider(color: Colors.blueGrey),
          ),
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
          const SizedBox(
            height: 5,
            width: 450,
            child: Divider(color: Colors.blueGrey),
          ),
          ListTile(
            leading: const CircleAvatar(
              backgroundColor: Colors.grey,
            ),
            title: Text(title),
            subtitle: Text(subtitle),
            onTap: () {
              // Update the state of the app
              // ...
              // Then close the drawer
              Navigator.pop(context);
            },
          ),
        ],
      ),
    );
  }
}
