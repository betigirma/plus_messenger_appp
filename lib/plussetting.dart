import 'package:flutter/cupertino.dart';

class PlussettingsPage extends StatelessWidget {
  const PlussettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('plus setting'));
  }
}
