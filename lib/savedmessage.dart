import 'package:flutter/cupertino.dart';

class SavedmessagesPage extends StatelessWidget {
  const SavedmessagesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('saved message'));
  }
}
