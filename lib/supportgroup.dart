import 'package:flutter/cupertino.dart';

class SupportgroupPage extends StatelessWidget {
  const SupportgroupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('support group'));
  }
}
